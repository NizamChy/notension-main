import React from "react";

const page = () => {
  //   const { currentCenter } = useSelector((state) => state.doctorInfo);

  //   console.log("currentCenter", currentCenter);

  return <div>getDoctorsInfoByCenter</div>;
};

export default page;
